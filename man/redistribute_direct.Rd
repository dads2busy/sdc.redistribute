% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/redistribute_direct.R
\name{redistribute_direct}
\alias{redistribute_direct}
\title{Area-weighted redistribution between polygon layers}
\usage{
redistribute_direct(
  source,
  target,
  extensive = NULL,
  intensive = NULL,
  preserve_totals = TRUE,
  suffix = NULL
)
}
\arguments{
\item{source}{An \code{sf} polygon layer carrying the values to redistribute.}

\item{target}{An \code{sf} polygon layer to estimate values for.}

\item{extensive}{Character vector of count column names in \code{source} to
redistribute as totals (area-share weighted, optionally rescaled to
preserve the source total).}

\item{intensive}{Character vector of rate/density column names in \code{source}
to redistribute as area-weighted means.}

\item{preserve_totals}{Logical; if \code{TRUE} (default) extensive results are
rescaled so each target column sums to the source total.}

\item{suffix}{Optional string appended to each new column name.}
}
\value{
The \code{target} layer (an \code{sf} object) with one new column per
redistributed measure.
}
\description{
Area-weighted redistribution between polygon layers
}
\details{
Extensive measures (counts) are redistributed by each intersection's share of
the source polygon area and, when \code{preserve_totals = TRUE}, rescaled so the
target totals match the source totals. Intensive measures (rates/densities)
are area-weighted means: the sum of each source value times the intersection's
share of the target polygon area (the standard areal-weighting intensive
estimator). This equals a true area-weighted mean when the target is fully
covered by the source and treats any uncovered part of a target as
contributing zero. \code{NA} source values are omitted from the weighted sums.
Targets that no source polygon covers receive \code{0} for extensive measures and
\code{NA} for intensive measures. If \code{target} already has a column named like a
redistributed measure, it is overwritten; pass \code{suffix} to keep both.
}
\examples{
src <- sf::st_sf(pop = 100, geometry = sf::st_sfc(
  sf::st_polygon(list(rbind(c(0,0), c(2,0), c(2,2), c(0,2), c(0,0)))),
  crs = 3857))
tgt <- sf::st_sf(id = c("A", "B"), geometry = sf::st_sfc(
  sf::st_polygon(list(rbind(c(0,0), c(1,0), c(1,2), c(0,2), c(0,0)))),
  sf::st_polygon(list(rbind(c(1,0), c(2,0), c(2,2), c(1,2), c(1,0)))),
  crs = 3857))
redistribute_direct(src, tgt, extensive = "pop")
}
